Convert the following README.md content into clean Blogger-compatible HTML.

Requirements:
- Use pure HTML (no Markdown).
- All C++ code must be inside <pre><code> blocks so it is fully copyable.
- Escape characters properly (e.g., < as &lt; and > as &gt; inside code).
- Keep headings structured with <h2>, <h3>, etc.
- Preserve formatting and indentation in the C++ code.
- Make it ready to paste directly into Blogger's HTML view.
- Do not include explanations — only return the final HTML output.

Here is the README.md content:

[PASTE YOUR README CONTENT HERE]

